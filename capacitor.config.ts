import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.krelk.financetracker',
  appName: 'FinanceTracker-App',
  webDir: 'dist'
};

export default config;
